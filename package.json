{
  "name": "game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Le Protégé : jeu coopératif en 3D, dans le navigateur, pour 4 amis.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --test-timeout=20000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
